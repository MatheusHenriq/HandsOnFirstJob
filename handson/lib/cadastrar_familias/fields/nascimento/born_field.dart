import 'package:flutter/material.dart';

class BornField extends StatelessWidget {
  const BornField({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.white,
      width: 100,
      height: 100,
      child: Center(
        child: Text('CAMPO DE NASCIMENTO, AINDA VOU FAZER'),
      ),
    );
  }
}
